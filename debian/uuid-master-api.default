# Defaults for uuid-master-api initscript
# sourced by /etc/init.d/uuid-master-api
# installed at /etc/default/uuid-master-api by the maintainer scripts

#
# This is a POSIX shell fragment
#

NODE_ENV=production

# Additional options that are passed to the Daemon.
DAEMON_OPTS=""
